<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:AuroraUI.Modules.MainMenu.Models"
             xmlns:viewModels="clr-namespace:AuroraUI.Modules.MainMenu.ViewModels"
             xmlns:converters="clr-namespace:AuroraUI.Framework.Converters"
              mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="AuroraUI.Modules.MainMenu.Views.MenuView">
  <Menu ItemsSource="{Binding Items}" x:DataType="viewModels:MenuViewModel">
    <Menu.Styles>
      <Style Selector="MenuItem" x:DataType="models:MenuItemBase">
        <Setter Property="Header" Value="{Binding Header}"/>
        <Setter Property="ItemsSource" Value="{Binding Items}"/>
        <Setter Property="Command" Value="{Binding Command}"/>
        <Setter Property="InputGesture" Value="{Binding KeyGesture}"></Setter>
        <Setter Property="IsVisible" Value="{Binding IsVisible}"></Setter>
        <Setter Property="Icon" Value="{Binding IconSource, Converter={x:Static converters:UriToImageConverter.Instance}}"/>
      </Style>
    </Menu.Styles>
  </Menu>
</UserControl>
